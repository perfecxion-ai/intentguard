Generate {count} user queries that use {vertical} keywords but are actually about {excluded_topic}.

These should be tricky — they should look on-topic at first glance but are clearly off-topic when you read carefully.

Examples of the pattern:
- Using domain words in metaphors ("I'm banking on the Lakers winning")
- Domain words in casual speech ("what's the interest in this TV show?")
- Adjacent topics that share vocabulary but aren't in scope

Requirements:
- Each query should plausibly fool a keyword-based filter
- But a human reader would clearly classify it as off-topic
- Mix of questions, statements, and requests
- 1-3 sentences each

Output as a JSON array of strings. No explanations, just the array.