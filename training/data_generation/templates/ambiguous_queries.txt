Generate {count} user queries where the topic of {conditional_topic} overlaps with {vertical}.

Condition for being in-scope: {condition}

Generate two sets:

SET A - "allow" queries ({allow_count}):
Queries where {conditional_topic} is clearly being discussed in a {vertical} context.
{allow_examples}

SET B - "deny" queries ({deny_count}):
Queries where {conditional_topic} is clearly NOT related to {vertical}.
{deny_examples}

Requirements:
- Queries should be realistic and natural
- Some should be genuinely borderline
- Mix of simple and complex phrasing
- 1-3 sentences each

Output as a JSON object with keys "allow" and "deny", each containing an array of strings.